{
  "version": 1,
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|Grep|Glob",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'b=\"${CHAOS_BIN:-$(command -v chaos 2>/dev/null)}\"; [ -x \"$b\" ] && exec \"$b\" hook --event PreToolUse --format cursor || exit 0'",
            "timeout": 15
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'b=\"${CHAOS_BIN:-$(command -v chaos 2>/dev/null)}\"; [ -x \"$b\" ] && exec \"$b\" hook --event PostToolUse --format cursor || exit 0'",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
